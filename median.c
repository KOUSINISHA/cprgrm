
#include <stdio.h>
#include<conio.h>
{
(int a[], int n)
int sum = 0;
for (int i = 0; i < n; i++) 
sum += a[i];
}
(int a[], int n)
{
sort(a, a+n);
if (n % 2 != 0)
}
int main()
{
    int a[] = { 1, 3, 4, 2, 7, 5, 8, 6 };
    printf("Enter the element");
    scanf("%d,&s");
    printf("print the median element");
    scanf("%d,&s");
    return 0;
}
